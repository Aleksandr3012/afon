mixin sPrayersList(data)
	// start sPrayersList
	+b.SECTION.sPrayersList.section#sPrayersList&attributes(attributes)
		.container
			+b.section-title.text-center
				h2 sPrayersList
				
			.row
	// end sPrayersList